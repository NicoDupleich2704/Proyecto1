<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zuckerberg.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fondoinicio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProductosAnimales"></activity>
        <activity android:name=".InicioAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TecnicosActivity" />
        <activity android:name=".acivityDoctores" />
        <activity android:name=".IniciarSesion" />
        <activity android:name=".TiposComidaActivity" />
        <activity android:name=".AnimalesActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>